<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".fragments.WorkoutFragment">

    <!-- Title -->


    <!-- Exercise Type Spinner -->
    <Spinner
        android:id="@+id/spinner_exercise_type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="16dp"
        android:prompt="@string/exercise_type_prompt"/>

    <!-- Muscle Group Spinner -->
    <Spinner
        android:id="@+id/spinner_muscle_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:prompt="@string/muscle_group_prompt"/>

    <!-- Difficulty Level Spinner -->
    <Spinner
        android:id="@+id/spinner_difficulty_level"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:prompt="@string/difficulty_level_prompt"/>

    <!-- Filter Button -->
    <Button
        android:id="@+id/button_filter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Exercises Instructions"
        android:layout_marginBottom="16dp"
        />

    <!-- RecyclerView to display exercises -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_exercises"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="8dp" />

</LinearLayout>
